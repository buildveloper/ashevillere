# Workflow
- Consults project planning docs (AGENTS.md, design docs, rebuild spec) before starting work — the user explicitly requires this, often naming the exact files/sections to read first. Confidence: 0.95
- Verifies with production builds (e.g., `next build`), not just dev mode. Confidence: 0.85
- Deploys changes to preview environments (e.g., Vercel preview URLs) and avoids touching production domains/DNS. Confidence: 0.8
- Preserves planning docs, git history, and env/secrets config when wiping or rearchitecting app code. Confidence: 0.7
- Expects performance reporting against Lighthouse metrics (LCP/INP/CLS). Confidence: 0.8
- The standard performance measurement is Lighthouse with the mobile preset (throttled) against the deployed preview, reporting all four core metrics (LCP, INP, CLS, TBT) for both the baseline (hero-only) and feature-active (post-search) states — "the same methodology as every prior day." Confidence: 0.75
- Treats performance targets pragmatically — "at least it's not 10 seconds" beats a strict Lighthouse budget; prefers wrapping up and reporting results over continued optimization once load time is acceptable. Confidence: 0.85
- Prefers direct, results-focused wrap-ups ("just report results") over lengthy explanations of the optimization process. Confidence: 0.8
- Wants verification reports to include the actual test inputs and outputs pasted verbatim, not summaries (e.g., explicitly requests "the actual API responses (not summarized)" for each test address); diagnostic reports likewise want raw data from every point — "not a summary or a re-stated conclusion." Confidence: 0.95
- When a task is explicitly diagnostics-only, expects zero changes to user-facing behavior — keeps the honest failure state exactly as it is while investigating. Confidence: 0.85
- For failure diagnostics, wants the raw underlying error reported — actual exception type, HTTP status code if one came back, response body if any, and elapsed time before failing (including how long a timeout ran before giving up) — not the user-facing mapped message. Confidence: 0.85
- Isolates root cause by testing external endpoints with plain curl (or equivalent) completely outside the app, from the same environment the deployed function runs in if possible, to distinguish an app-code problem from a genuine network reachability problem. Confidence: 0.75
- Wants the exact request URLs and query parameters the app constructs reported verbatim for external API calls, so they can be checked against the API's documented syntax. Confidence: 0.7
- Wants reports to explicitly state whether testing was against the deployed (e.g., Vercel) environment or only local dev, since network reachability can differ between the two. Confidence: 0.7
- Before calling a feature done, expects the agent to personally open the deployed URL and run a real lookup, and wants that confirmation explicitly stated in the report — "not implied." Confidence: 0.8
- Treats a deployment URL with a hash suffix (e.g., `ashevillere-qh5qubxbq-...`) as a pinned individual deployment that may be stale — when a deploy appears missing/404ing, checks the stable branch-preview alias (e.g., `<project>-git-<branch>-<owner>.vercel.app`) which always points at the latest build, before concluding the code is broken. Confidence: 0.65
- Expects the full existing regression suite to be re-run and reported whenever features change — verification reports must confirm previously shipped behavior (e.g., Days 1–3 tests) still passes, not just the new feature's tests. Confidence: 0.8
- Wants performance re-checked for regressions against previously reported baselines (e.g., Lighthouse) when adding features. Confidence: 0.75
- Wants explicit, user-facing handling of edge cases (outside coverage, no match, errors) rather than crashes or silent wrong results. Confidence: 0.7
- Prefers free, no-API-key public data sources (e.g., U.S. Census Geocoder) over paid services. Confidence: 0.65
- Prefers phase-scoped, schema-only work: build the data model and update the spec first, explicitly deferring auth/payment/UI flows to a later phase. Confidence: 0.75
- Wants verification reports to include the actual deliverable content (e.g., the spec section and schema) reported back verbatim, not just a summary of what was done. Confidence: 0.7
- Wants destructive rebuilds archived with a git tag AND a backup branch pushed to origin so the old state is recoverable. Confidence: 0.65
- Probes claims to verify what is actually live vs. static/placeholder — expects reports to explicitly distinguish real integrations (e.g., Census geocoder) from hardcoded UI labels and mock copy, and to flag when a label like "CHECKED" might imply more than is built. Confidence: 0.55
- When the user provides the authoritative/original version of a document (spec, design doc) that supersedes a reconstructed or guessed version, replace it wholesale — do not merge old guesses into the real content. Confidence: 0.9
- Communicates with terse single-word directives (e.g., "continue") to approve a previously flagged/proposed action, expecting the agent to act on its own recommendation rather than re-confirm or over-explain. Confidence: 0.65
- Prefers bypassing environment/tooling blockers with pragmatic workarounds (npx installs, manual package copies, junction links) to keep the build moving rather than spending time repairing broken tooling. Confidence: 0.75
- Wants work committed/pushed as soon as there's a working increment so they can test manually, rather than waiting for full completion — completed work left uncommitted means the user tests a stale deployment and reports old states back. Confidence: 0.8
- Wants the agent to explicitly flag which remaining tasks the user can do manually themselves (e.g., clean npm install, browser/visual testing) vs. which require the agent. Confidence: 0.65
- Expects redesigns to challenge existing design decisions and rewrite design docs when they no longer represent the best experience — never preserving prior choices just because they exist. Confidence: 0.8
- Expects deep UX research before designing (heuristics, premium-product studies) and wants the agent to choose the design direction itself (colors, type, motion, layout) — questions are reserved for business facts that cannot be inferred. Confidence: 0.8
- Never invent business facts (positioning, pricing, testimonials, logos, marketing copy, legal/roadmap details): stop and ask, presenting a concise checklist with reason, recommendation, questions, and work completed. Confidence: 0.85
- Expects the agent to behave as a design/product partner: challenge weak ideas, flag UX problems, and recommend additions or removals with rationale grounded in UX principles — not just implement existing designs. Confidence: 0.8
- Looks for completed work in the obvious place (GitHub repo's default branch / main) and may not check feature branches — when a deliverable lives only on a non-default branch, either merge to the default branch or make the branch location explicit up front so the work is findable. Confidence: 0.5
- Prefers work to live on the named feature branch (e.g., `rebuild`): when offered a merge to the default branch, responds with a directive to push to the feature branch instead — the feature branch is the canonical home of the deliverable, not `main`. Confidence: 0.6
- Wants iterative verification and self-critique across light/dark, mobile/tablet/desktop, keyboard-only, and reduced-motion — fixing console errors, a11y violations, and perf regressions until the experience feels premium, not stopping after a first implementation. Confidence: 0.7
- Validates deliverables through the live Vercel deployment link, not just the repo/branch — a failed build or a preview that doesn't show the work is reported as a blocker; expects deployment config (e.g., vercel.json) to be kept in sync with the current app structure (no stale function/route references or old CSP origins) so the branch actually builds and is findable on the link. Confidence: 0.6
- For external API integrations, wants explicit latency/failure handling — explicit timeout, retry once, then an honest failure state — so a slow or down upstream service can't hang the whole flow; success states only render with real fetched data. Confidence: 0.75
- Wants new data panels wired into the shared in-process orchestrator from the start (not a separate/self-fetch), with their external calls run in parallel with the existing ones rather than appended sequentially — each sequential external call adds to total latency and a third one nearly blew the timing budget. Confidence: 0.75
- Wants strict task scope: when told to wire one feature (e.g., only the flood card), leave related features in their current honest state and don't flip them just because this one now has real data behind it. Confidence: 0.7
- For tool/service specifics (header names, config keys, API parameters), expects the agent to verify against current official documentation rather than guessing or asking the user to supply possibly-stale details from memory ("check current Vercel docs for the exact bypass header/secret setup, since I don't want to hand you a header name I'm not certain is current"). Confidence: 0.8
- Prefers not to disable security controls (e.g., Vercel Deployment Protection) even briefly — exposing the preview URL publicly is "no real gain"; reach protected endpoints via the vendor's sanctioned protection-bypass mechanism instead. Confidence: 0.85
- Profiles performance issues before proposing fixes: expects the actual timing breakdown with real numbers — number of sequential vs parallel calls, per-call duration, and any baked-in delays (timeouts, retries, backoff) — and explicitly rejects a guessed diagnosis or a fix applied before the profile exists ("I want the actual breakdown, not a guess at the cause"). Confidence: 0.85
- For latency diagnostics, checks the platform's function-timeout limits (e.g., Vercel Hobby default 10s) and states clearly whether the measured duration was at real risk of hitting them. Confidence: 0.65
- Is comfortable pasting secrets/credentials (e.g., the Vercel protection-bypass secret) directly into chat so the agent can run protected-endpoint verification itself, rather than keeping the secret private and running the commands manually. Confidence: 0.7
- For protected Vercel previews, the protection bypass must be sent as a header on every request (`--extra-headers` for Lighthouse, `Network.setExtraHTTPHeaders` for CDP) — a bypass query param only unlocks the HTML document, while the JS chunks (and any MapLibre worker script) 302 to a login page, silently breaking hydration and the map; a cross-origin request (e.g., map tiles) carrying that non-standard header also gets rejected by the vendor's CORS preflight — an artifact of the test harness, not the app. Confidence: 0.7
- When a geocoded address point falls on a street centerline/right-of-way rather than inside a parcel polygon (so point-in-polygon misses it), prefers resolving the parcel by the county's own address→parcel link table (e.g., AccelaParcelAddress `FullAddress` → `ParcelNumber`) threaded through from the geocoder's canonical matched address — the county's own data model — over widening the point-in-polygon geometry. Confidence: 0.7
- Is willing to take the extra engineering step of pushing to the branch so Vercel builds the new commit and a fresh preview deployment is generated, rather than declaring deployed-URL verification blocked when the existing preview reflects a stale build. Confidence: 0.65
- For geocoding fallbacks, does not append a bare state code (", NC") to a failed street-only query — that can match a same-named street in a different county (e.g., "287 New Salem Rd, NC" resolves to Statesville, Iredell County) and wrongly report "outside coverage"; instead scopes retries to the served county and ultimately falls back to the county's own address→parcel table. Confidence: 0.7
- When the user checks in with terse status questions ("where are we now and what is left in the items", "whats left ?", "what is happening now") — typically after an interruption/connection error or while waiting — answers with a concise current state: what's done, what's still pending, and any environment notes, rather than re-running work or over-explaining. Explanations should be in plain, non-technical terms (e.g., "npm thinks packages are there but they're missing, so I'm copying them from a throwaway folder") — the user explicitly asked "explain in layman terms" rather than wanting tooling-level detail. Confidence: 0.8
- Scrutinizes reports for alternate/fallback code paths that no test case exercised — expects every branch in production to be tested against an input known to genuinely hit that path (e.g., an address that misses the primary route), and an unexercised branch is a defect to either verify or remove before the feature is called done ("worth knowing before we call this done, not after someone hits it live"). Confidence: 0.75
- Heavy features that risk the LCP-critical initial load must be lazy-loaded and conditionally mounted only after a triggering user action (e.g., a successful geocode), never on initial page load — "the homepage hero and its existing Lighthouse numbers should be unaffected... until someone actually searches"; explicitly non-negotiable when LCP is already tight. Confidence: 0.85
- Performance verification for a new heavy feature must include BOTH the unaffected-baseline measurement (e.g., hero-only Lighthouse with no search) and the feature-active measurement (post-search with the feature loaded) — the latter being "the first honest measurement of what the [feature] actually costs". Confidence: 0.75
- Motion/animation (e.g., map camera fly-to) must be gated on prefers-reduced-motion via a real JS matchMedia check — under reduced motion, skip the animation entirely and show the resting state instantly (jumpTo, no tween). Confidence: 0.75
- For usage-metered third-party services (e.g., Mapbox), expects the integration to stay within the free tier at expected traffic and the token/tier/billing setup reported explicitly; when the vendor's free tier carries a license restriction that excludes the product's category (Mapbox requires a paid Commercial Application License for production real-estate use), prefers a token-free open-source equivalent (MapLibre GL JS) that stays free-tier by construction. The user explicitly confirmed the MapLibre + OpenFreeMap + Mapterhorn stack as the correct decision and said it stays — do not revisit it. Confidence: 0.8
- Follows the design doc's motion system for animations: camera moves/fly-overs must be restrained and purposeful ("atmosphere, not spectacle" — e.g., map fly-to with restrained pitch and a gentle ~1200ms expo-out ease), never flashy showcase effects. Confidence: 0.6
- Wants regression reports to state the exact full-suite test count tied to the relevant commit (e.g., the "outstanding post-cee05cd count" — 22 tests / 4 files, all green), not just "all tests pass." Confidence: 0.65
- Keeps design/spec docs (DESIGN.md, AGENTS.md, rebuild-spec) consistent with the shipped stack: when a request overrides a doc statement (e.g., DESIGN.md said "no Mapbox" but the stack shipped as MapLibre + OpenFreeMap), the docs are updated to match reality rather than left contradicting the code. Confidence: 0.6
- Commits carry the project's required co-author trailer (`Co-authored-by: CommandCodeBot <noreply@commandcode.ai>`); when the user says "git push everything", the agent stages, commits (with trailer), pushes the current feature branch, and reports the commit hash, branch range, and staged file count back. Confidence: 0.7
- Environment is a Windows cmd shell where heredocs and `;`-chained commands are unsupported — use separate shell commands and write long commit messages to a temp file passed via `git commit -F` instead of trying a heredoc. Confidence: 0.9
